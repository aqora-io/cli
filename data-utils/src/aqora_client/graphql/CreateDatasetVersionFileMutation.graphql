mutation CreateDatasetVersionFileMutation(
  $datasetVersionId: ID!
  $partitionNum: Int!
) {
  createDatasetVersionFile(
    datasetVersionId: $datasetVersionId
    partitionNum: $partitionNum
  )
}
