mutation UpdateUseCaseMutation($competition_id: ID!, $pyprojectToml: String!) {
  updateUseCase(
    competitionId: $competition_id
    input: { pyprojectToml: $pyprojectToml }
  ) {
    node {
      id
      files {
        kind
        uploadUrl
      }
    }
  }
}
