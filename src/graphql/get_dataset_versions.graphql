query GetDatasetVersions($owner: String!, $localSlug: String!, $limit: Int) {
  datasetBySlug(owner: $owner, localSlug: $localSlug) {
    versions(first: $limit) {
      nodes {
        version
        publishedAt
        updatedAt
        size
      }
    }
  }
}
