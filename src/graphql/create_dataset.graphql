mutation CreateDataset($owner: UsernameOrID, $input: CreateDatasetInput!) {
  createDataset(asEntity: $owner, input: $input) {
    id
    localSlug
    owner {
      __typename
      username
    }
    viewerCanCreateVersion: viewerCan(action: CREATE_DATASET_VERSION)
  }
}
