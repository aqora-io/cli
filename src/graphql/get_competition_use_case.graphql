query GetCompetitionUseCase($id: ID!) {
  node(id: $id) {
    __typename
    ... on Competition {
      useCase {
        version
        pyprojectToml
        files {
          kind
          downloadUrl
        }
      }
    }
  }
}
