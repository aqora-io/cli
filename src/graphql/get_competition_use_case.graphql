query GetCompetitionUseCase($slug: String!) {
  competitionBySlug(slug: $slug) {
    id
    useCase {
      latest {
        version
        pyprojectToml
        files {
          kind
          downloadUrl
        }
      }
    }
  }
}
