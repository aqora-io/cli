query GetCompetitionUseCase($id: ID!) {
  node(id: $id) {
    __typename
    ... on Competition {
      useCase {
        latest {
          version
          pyprojectToml
          files {
            kind
            downloadUrl
          }
        }
      }
    }
  }
}
