query GetViewerRelatedEntities($action: Action!) {
  viewer {
    __typename
    id
    username
    displayName
    ... on User {
      entities(permission: { to: $action }) {
        nodes {
          __typename
          id
          username
          displayName
        }
      }
    }
  }
}
