mutation CreateStoreCredentials($input: CreateStoreCredentialsInput) {
  createStoreCredentials(input: $input) {
    accessKeyId
    secretAccessKey
    expiresAt
    endpoint
    bucket
    region
  }
}
